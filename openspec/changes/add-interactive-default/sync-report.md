# Sync Report: add-interactive-default

## Documentation

- README documents `agentos` and forwarded no-subcommand options.
- Architecture documents the console-script wrapper and interactive dispatch.
- Roadmap and implementation status mention interactive default mode.

## Memory

- No memory update requested.

## Repository

- Local changes verified with `pytest`, `ruff check .`, `agentos version`, and no-subcommand interactive execution through the installed command.
